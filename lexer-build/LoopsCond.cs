namespace Test {
    public class Program {
        public static void Main() {
            // Простые выражения
            int x = 5;
            int y = 10;
            
            // Условие
            if (x < y) {
                x = x + 1;
            }
            
            // Цикл
            while (x < 20) {
                x = x + 1;
            }
            
            // Возврат
            return;
        }
    }
}